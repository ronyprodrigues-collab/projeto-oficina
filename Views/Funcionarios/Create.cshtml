@model Models.ViewModels.CreateFuncionarioViewModel
@{
    ViewData["Title"] = "Cadastro de Funcionário";
}

<h1>Cadastro de Funcionário</h1>

<div class="row">
    <div class="col-md-6">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="mb-3">
                <label class="form-label">Nome Completo</label>
                <input asp-for="NomeCompleto" class="form-control" required />
            </div>
            <div class="mb-3">
                <label class="form-label">E-mail</label>
                <input asp-for="Email" class="form-control" type="email" required />
            </div>
            <div class="mb-3">
                <label class="form-label">Cargo</label>
                <select asp-for="Cargo" class="form-select" required>
                    <option value="">Selecione...</option>
                    <option>Supervisor</option>
                    <option>Mecanico</option>
                </select>
            </div>
            <div class="mb-3">
                <label class="form-label">Senha temporária</label>
                <input asp-for="Senha" class="form-control" type="password" value="P@ssw0rd!" />
                <div class="form-text">Será exigida troca posteriormente.</div>
            </div>
            <div class="mt-3">
                <button type="submit" class="btn btn-success">Salvar</button>
                <a asp-action="Index" class="btn btn-secondary">Voltar</a>
            </div>
        </form>
    </div>
</div>
